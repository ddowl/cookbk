<h1>New Recipe</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.user_recipe_path(@conn, :create, @current_user)) %>

<span><%= link "Back", to: Routes.user_recipe_path(@conn, :index, @current_user) %></span>
